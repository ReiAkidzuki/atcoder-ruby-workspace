[project]
name = "atcoder-ruby-tools"
version = "0.1.0"
description = "Local command-line tools for the AtCoder Ruby workspace"
requires-python = ">=3.10,<4"
dependencies = [
  "aclogin",
  "online-judge-tools @ git+https://github.com/online-judge-tools/oj.git@v12.0.0",
]

[tool.uv]
package = false
